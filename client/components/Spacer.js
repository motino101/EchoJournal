import { Box } from '@gluestack-ui/themed';

/**
 * Spacer Component
 *
 * A reusable element which takes up space but is transparent.
 *
 * @owner Anna Hudson
 *
 * @returns A React element representing the Spacer component.
 */
export default function Spacer(props) {
  return <Box {...props} />;
}
